/* 76. JavaScript Do-While Loop:
The do-while loop is a variation of the while loop a do while loop will always execute the entire code block once before checking if a condition is met if the condition is met. After the first pass it will then loop the code block as long as the condition remains true.

Example:
*/

function myFunction() {
    var i = 0;
    do {
        document.write(i+"<br>");
        i++;
    }
    while (i < 10){
        document.getElementById('doWhile').innerHTML = text;       
    }
}